obstacle_range: 2.5
raytrace_range: 3.0

inflation_radius: 0.25

footprint: [[-0.055,-0.055],[-0.055,0.055],[0.055,0.055],[0.055,-0.055]]

observation_sources: kinect_scan_marking kinect_scan_erasing

# PointcloudtoLaserscan is no use?
# We do not make /scan_marking which is using self filter, because the kinect put on the beego only get pointcloud in front of beego.

#kinect_scan_marking: {sensor_frame: /openni_depth_optical_frame, topic: /scan, data_type: LaserScan, expected_update_rate: 0.6, observation_persistance: 0.0, marking: true, clearing: true}

kinect_scan_marking: {sensor_frame: /openni_depth_optical_frame, topic: /camera/depth/points, data_type: PointCloud2, expected_update_rate: 0.6, observation_persistance: 0.0, marking: true, clearing: false, min_obstacle_height: 0.08, max_obstacle_height: 0.4}

kinect_scan_erasing: {sensor_frame: /openni_depth_optical_frame, topic: /camera/depth/points, data_type: PointCloud2, expected_update_rate: 0.2, observation_persistance: 0.0, marking: false, clearing: true, min_obstacle_height: -0.1, max_obstacle_height: 0.4}

#max_obstacle_height: 0.3

